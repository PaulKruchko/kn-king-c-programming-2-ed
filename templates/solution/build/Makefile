# Copy this directory beside a src/ directory for a new solution.

TARGET  = solution.out
SOURCES = main.c
CSTD    = c99

# Add libraries when required, for example:
# LDLIBS = -lm

include ../../../mk/solution.mk
