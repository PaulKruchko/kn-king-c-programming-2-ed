#include <stdio.h>

/* Computes the dimensional weight of a box from user input */

int main(void)
{
    int height, length, weight, volume, weight;

    printf("Enter height of a box: ");
    scanf("%d", &height);
    
    return 0;
}
